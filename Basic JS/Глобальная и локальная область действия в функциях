Глобальная и локальная область действия в функциях
Можно иметь как локальные , так и глобальные переменные с одинаковым именем. При этом локальная переменная имеет приоритет над глобальной переменной.

В этом примере:

const someVar = "Hat";

function myFun() {
  const someVar = "Head";
  return someVar;
}
Функция myFunвернет строку Head, поскольку присутствует локальная версия переменной.
